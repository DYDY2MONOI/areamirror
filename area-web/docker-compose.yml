version: '3.8'

services:
  # Service de développement
  area-web-dev:
    build:
      context: .
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev -- --host 0.0.0.0

  # Service de production
  area-web-prod:
    build:
      context: .
      target: production
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    profiles:
      - production

  # Service de build (pour construire sans lancer)
  area-web-build:
    build:
      context: .
      target: builder
    volumes:
      - ./dist:/app/dist
    profiles:
      - build
